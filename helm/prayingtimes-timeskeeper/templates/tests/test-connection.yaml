apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prayingtimes-timeskeeper.fullname" . }}-test-connection"
  labels:
    {{- include "prayingtimes-timeskeeper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prayingtimes-timeskeeper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
